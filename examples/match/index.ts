/**
 * match() Examples Index (Stable)
 *
 * Run:
 *   bun run examples/match/01-stable-match.ts
 */

/**
 * 01-stable-match.ts
 *
 * Topics covered:
 * - match() with stable Option/Result
 * - P helpers and guarded cases
 * - wildcard/when/otherwise
 * - generic discriminated unions
 * - async-inner stable values via toPromise()
 */

export {}
